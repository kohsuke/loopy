iso9660 can contain multiple volume descriptors (primary and 0+ secondaries),
and each specifies a different view of the file system.

Joliet uses this to define a UCS names, which this library attempts to use, yet RockRidge uses
the primary and just adds extra attributes.